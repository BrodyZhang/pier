<div class="card" style="max-width:600px;margin:40px auto;">
    <h1>创建告白页请求</h1>
    <p style="color:rgba(255,255,255,0.5);margin-bottom:20px;">描述你想要的页面风格和内容，我们的团队会审核并手动制作。</p>
    <div class="disclaimer">
        本项目为学习项目，不涉及任何 AI。您的页面将由我们的团队手动创建。数据可能随时被清除。
    </div>
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="error"><%= error %></div>
    <% } %>
    <form method="POST">
        <label>页面名称 <span style="color:rgba(255,255,255,0.3);font-size:12px;">（最多50字）</span></label>
        <input type="text" name="name" required maxlength="50" placeholder="例如：给我的爱人">
        <label>详细描述 <span class="char-count" id="descCount">0/500</span></label>
        <textarea name="description" rows="6" required maxlength="500" placeholder="描述你想要的页面风格、色调、特殊寄语等…" oninput="document.getElementById('descCount').textContent = this.value.length + '/500';"></textarea>
        <button type="submit" class="btn btn-primary" style="margin-top:20px;width:100%;justify-content:center;">提交请求</button>
    </form>
</div>
